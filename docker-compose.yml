version: "3.9"

services:
  web:
    image: nginx:1.21.3-alpine
    ports:
      - "8080:80"
    volumes:
      - .:/code
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    links:
      - php

  php:
    image: php:7-fpm
    volumes:
      - .:/code
